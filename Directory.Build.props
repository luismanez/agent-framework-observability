<Project>
  <PropertyGroup>
    <!-- Repo root resolved from the location of this file (always at repo root) -->
    <RepoRoot>$(MSBuildThisFileDirectory)</RepoRoot>
  </PropertyGroup>

  <Import Project="$(MSBuildThisFileDirectory)eng/Directory.Build.props" />
</Project>
